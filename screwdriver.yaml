shared:
   image: node:8
jobs:
    simple:
        annotations:
          screwdriver.cd/buildPeriodically: H * * * * 
        steps:
            - echo: echo test
        requires: [~commit]
    parallel:
        steps:
            - echo: echo test
        requires: [simple]
    join:
        steps:
            - echo: echo test
        requires: [parallel, sd@1:external]
